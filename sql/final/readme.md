# Final SQL Assignment

This file contains a complete mini college database script in `final.sql`.

## What it includes
- Table creation (`students`, `courses`, `instructors`, `enrollments`, `departments`)
- Sample data inserts
- Basic SQL operations (`SELECT`, `UPDATE`, `DELETE`)
- Practice queries with joins, grouping, subqueries, window function, and `CASE`


